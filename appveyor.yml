version: "{build}"
build: off
shallow_clone: true
skip_tags: true

environment:
  matrix:
    - nodejs_version: 0.10

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

test_script:
  - node --version
  - npm --version
  - npm t
